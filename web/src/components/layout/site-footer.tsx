export function SiteFooter() {
  return (
    <footer className="border-t border-stone-800/80 bg-stone-950/80 backdrop-blur-xl">
      <div className="mx-auto flex w-full max-w-6xl flex-col gap-3 px-6 py-6 text-[0.6rem] uppercase tracking-[0.3em] text-stone-500 sm:flex-row sm:items-center sm:justify-between">
        <span>Deckard Avatar Studio</span>
        <span>Crafted for realtime clone operations</span>
      </div>
    </footer>
  );
}
